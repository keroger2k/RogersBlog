%h1 Posts

%table.posts-table
  %tbody
    -@posts.each do |post|
      %tr
        %td.date
          %p
            =post.created_at.strftime("%d %B, %Y")
        %td.title
          =link_to(post.title, post_path(post))
        %td.summary
          %p
            =truncate(post.body, :length => 120)
        %td.info
          Comment Count
